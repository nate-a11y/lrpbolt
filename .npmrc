# Force npm to use recognized proxy variables everywhere
proxy=http://proxy:8080
https-proxy=http://proxy:8080

# Optional: If using self-signed or corporate certs
strict-ssl=false

# Ensures npm doesn’t trip on undefined old vars
registry=https://registry.npmjs.org/
always-auth=false
